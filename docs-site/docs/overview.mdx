---
title: Convert-Pheno Documentation
sidebar_label: Overview
---

import Link from '@docusaurus/Link';

<div className="convertDocHero">
  <p className="convertEyebrow">Overview</p>
  <h2>Clinical and phenotypic data conversion across supported exchange models.</h2>

  Convert-Pheno supports conversion routes involving [BFF](bff), [PXF](pxf),
  [OMOP-CDM](omop-cdm), [REDCap](redcap), [cBioPortal](cbioportal), [CDISC-ODM](cdisc-odm),
  [CDISC Dataset-JSON](dataset-json), [CDISC Dataset-XML](dataset-xml),
  [FHIR R4 and mCODE](fhir), [i2b2](i2b2), [PCORnet CDM](pcornet),
  [Sentinel CDM](sentinel), [CSV](csv), and
  [openEHR](openehr).

  <div className="convertHeroActions">
    <Link className="button button--primary" to="/quickstart">Run your first conversion</Link>
    <Link className="button button--secondary" to="/conversion-recipes">Choose a conversion</Link>
    <Link className="button button--secondary" to="/download-and-installation">Install</Link>
  </div>
</div>

:::note[Project status]
This documentation tracks the development version `Convert-Pheno 0.34_1`.

The established routes are used in research projects and covered by regression
tests. Routes marked experimental have been tested with fewer independent
datasets; their format guides state the current coverage.

:::
:::tip[Choose an interface]
Start with the command-line interface. It supports files, mapping files, REDCap
dictionaries, OMOP tables, terminology reports, and multi-entity BFF output.

From **Convert-Pheno 0.35**, the local [Workbench](graphical-interface) provides
a browser interface for supported JSON and file-based conversions. The API and
module are intended for integration with other software.
:::
## Typical conversions

Most users come to `Convert-Pheno` for one of these tasks:

- convert `PXF` to `BFF` `individuals`, `biosamples`, `datasets`, and `cohorts`
- convert `OMOP-CDM` exports to `BFF` or `PXF`
- convert `REDCap` or `CDISC-ODM` data through a **mapping file**
- convert CDISC Dataset-JSON SDTM domains to `BFF`, `PXF`, or supported `OMOP-CDM` tables
- resolve CDISC Dataset-XML SDTM domains with Define-XML and convert them to `BFF`, `PXF`, or supported `OMOP-CDM` tables
- convert generic FHIR R4 or mCODE 4.0 Bundles to `BFF`, `PXF`, or supported `OMOP-CDM` tables
- convert cBioPortal clinical study packages to entity-aware `BFF`, `PXF`, or supported `OMOP-CDM` tables
- convert exported i2b2, PCORnet CDM, or Sentinel CDM tables to `BFF`, `PXF`, or supported `OMOP-CDM` tables
- use mapped raw `CSV` as an ETL source for `BFF`, `PXF`, or supported `OMOP-CDM` tables
- convert `BFF` to `PXF`, flattened `CSV`, flattened `JSON`, or `JSON-LD`

## Where to start

- [5-Minute Quickstart](quickstart) runs one small PXF-to-BFF conversion
- [Choose a Conversion](conversion-recipes) lists commands by input format
- [Supported Formats](supported-formats) summarizes accepted files and outputs
- [Mapping Files](mapping-files) explains project-specific CSV, REDCap, and ODM mappings
- [Workbench](graphical-interface) covers the local browser interface available from version 0.35

## Developer interfaces

- [Module](use-as-a-module) for local Perl or Python code
- [API](use-as-an-api) for HTTP(s) requests and the OpenAPI contract
- [Implementation](implementation) for the route registry and execution model
- [Troubleshooting](troubleshooting) for common installation and input errors

## External components

- [GitHub repository](https://github.com/CNAG-Biomedical-Informatics/convert-pheno)
