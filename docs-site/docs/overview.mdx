---
title: Convert-Pheno Documentation
sidebar_label: Overview
---

import Link from '@docusaurus/Link';

<div className="convertDocHero">
  <p className="convertEyebrow">Overview</p>
  <h2>Clinical and phenotypic data conversion across supported exchange models.</h2>

  Convert-Pheno supports conversion routes involving [BFF](bff), [PXF](pxf),
  [OMOP-CDM](omop-cdm), [REDCap](redcap), [cBioPortal](cbioportal), [CDISC-ODM](cdisc-odm),
  [CDISC Dataset-JSON](dataset-json), [CDISC Dataset-XML](dataset-xml),
  [FHIR R4 and mCODE](fhir), [CSV](csv), and
  [openEHR](openehr).

  <div className="convertHeroActions">
    <Link className="button button--primary" to="/quickstart">Run your first conversion</Link>
    <Link className="button button--secondary" to="/conversion-recipes">Choose a conversion</Link>
    <Link className="button button--secondary" to="/download-and-installation">Install</Link>
  </div>
</div>

:::note[Project status]
This documentation tracks the development version `Convert-Pheno 0.34_1`.

The software is in active use and still evolving. Some conversion routes are more mature than others, so it is worth checking the format-specific pages before running a new conversion on real data.

:::
:::tip[Choose an interface]
The command-line interface remains the main entry point for normal use. It supports real files, mapping files, REDCap dictionaries, OMOP tables, terminology reports, and multi-entity BFF output.

From **Convert-Pheno 0.35**, the local [Workbench](graphical-interface) provides a browser interface for supported JSON and file-based conversions. The API and module interfaces are mainly for developers who need to call Convert-Pheno from another application or script.
:::
## Typical conversions

Most users come to `Convert-Pheno` for one of these tasks:

- convert `PXF` to `BFF` `individuals`, `biosamples`, `datasets`, and `cohorts`
- convert `OMOP-CDM` exports to `BFF` or `PXF`
- convert `REDCap` or `CDISC-ODM` data through a **mapping file**
- convert CDISC Dataset-JSON SDTM domains to `BFF`, `PXF`, or supported `OMOP-CDM` tables
- resolve CDISC Dataset-XML SDTM domains with Define-XML and convert them to `BFF`, `PXF`, or supported `OMOP-CDM` tables
- convert generic FHIR R4 or mCODE 4.0 Bundles to `BFF`, `PXF`, or supported `OMOP-CDM` tables
- convert cBioPortal clinical study packages to entity-aware `BFF`, `PXF`, or supported `OMOP-CDM` tables
- use mapped raw `CSV` as an ETL source for `BFF`, `PXF`, or supported `OMOP-CDM` tables
- convert `BFF` to `PXF`, flattened `CSV`, flattened `JSON`, or `JSON-LD`

## Start Here

- [5-Minute Quickstart](quickstart) if you want to complete one small conversion now
- [Choose a Conversion](conversion-recipes) if you want a route finder and copy-paste commands for common conversions
- [Workbench](graphical-interface) if you want a local browser interface, available from Convert-Pheno 0.35
- [Supported Formats](supported-formats) if you need accepted files and format-specific behavior

:::note[Workbench, API, and CLI]
Use the **CLI** for automation, streaming, or large datasets. Use the local **Workbench** for interactive conversions and terminology review.

Use the HTTP(s) API when integrating Convert-Pheno into another application. The Workbench calls this API but presents its inputs and results as a guided workflow.

:::
## Documentation map

- [Command-Line Interface](use-as-a-command-line-interface) if you want to **run the tool now**
- [Workbench](graphical-interface) if you prefer a **local browser interface**
- [Mapping Files](mapping-files) for an **end-to-end mapped REDCap workflow**
- [Use as an API](use-as-an-api) if you want the **HTTP(s) contract, API support matrix, and JavaScript fetch examples**
- [Troubleshooting](troubleshooting) if something is not behaving as expected
- [Implementation](implementation) if you want the **architectural view**

## External components

- [GitHub repository](https://github.com/CNAG-Biomedical-Informatics/convert-pheno)
