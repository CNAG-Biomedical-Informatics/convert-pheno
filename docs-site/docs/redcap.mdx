---
title: REDCap
sidebar_label: REDCap
---

import useBaseUrl from '@docusaurus/useBaseUrl';

export const RedcapFormatImage = () => (
  <img
    src={useBaseUrl('/img/redcap-format.svg')}
    alt="REDCap data, dictionary and mapping inputs producing a normalized record"
    className="convertFormatDiagram"
  />
);

:::note[Implementation and validation status]
REDCap conversion is implemented and covered by regression tests, including
data-dictionary and mapping-file processing. Because projects are free-form,
review the project mapping, optional ontology-search audit, and generated
output. Independent validation across diverse REDCap projects remains ongoing.
:::

**REDCap** stands for **R**esearch **E**lectronic **D**ata **Cap**ture. See the
REDCap [documentation](https://www.project-redcap.org).

<div className="convertFormatSummary" aria-label="At a glance">
  <div><span>Role</span><strong>Input</strong></div>
  <div><span>Accepted input</span><strong>CSV export, dictionary, mapping file</strong></div>
  <div><span>Main output</span><strong>BFF, PXF, OMOP-CDM</strong></div>
  <div><span>Notes</span><strong>Keep exports as UTF-8 text files</strong></div>
</div>

<figure className="convertFormatFigure">
<RedcapFormatImage />
<figcaption>Project data and dictionary metadata are interpreted through the user mapping</figcaption>
</figure>

## REDCap As Input

REDCap projects are free-form: project authors choose variable names, field
types, coded values, and event structures. Convert-Pheno therefore needs three
project files:

1. A REDCap data export in CSV format
2. The REDCap data dictionary in CSV format
3. A Convert-Pheno mapping file in YAML or JSON

Export all records and fields. Gzip-compressed CSV inputs are accepted. A
REDCap CDISC-ODM export follows the separate [CDISC-ODM](cdisc-odm) route.

:::warning[Keep REDCap text exports as UTF-8 text files]
Do not open and resave REDCap CSV exports or dictionaries with spreadsheet
software such as Excel before conversion. This can alter UTF-8 text and corrupt
characters such as `µ`, `≥`, accents, or degree symbols, breaking dictionary
values and ontology mappings.
:::

The mapping file defines how project variables become Beacon individuals and,
when configured, first-class biosamples. Start with
[Working with Mapping Files](mapping-files), then use the complete
[Mapping File](tbl/mapping-file) reference for additional keys.

Use [REDCap conversion recipes](conversion-recipes#redcap-input-bff-output) for
BFF, PXF, and OMOP-CDM commands.

### Output Review

Generated BFF records preserve the original source row under
`info.REDCap_columns` by default. Use `--no-source-info` when that copy is not
needed.

`--search-audit-tsv` records the source row, original and converted labels,
converted identifier, ontology, match status, match source, and effective
search settings. Review this file when project terms are resolved through the
bundled SQLite ontology databases.

Mapping-file routes can also emit biosamples defined by
`beacon.biosamples.rules` and synthesize Beacon `datasets` and `cohorts`.
Dataset and cohort metadata can be set under their `defaults` sections.

<details>
<summary>Longitudinal REDCap data</summary>

REDCap event information is retained under BFF `info` because the Beacon v2
individuals model has no direct longitudinal event structure.

</details>

<details>
<summary>REDCap API support</summary>

Convert-Pheno reads REDCap exports; it does not retrieve records through the
REDCap API. The Convert-Pheno HTTP(s) API can receive file paths, but the CLI is
normally more practical when data, dictionary, and mapping files are required.

</details>
